{
  "name": "@mensaly/database",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "generate": "prisma generate",
    "migrate:dev": "prisma migrate dev",
    "typecheck": "tsc --noEmit",
    "lint": "tsc --noEmit",
    "test": "echo No database tests configured yet"
  },
  "dependencies": {
    "@prisma/client": "^6.7.0"
  },
  "devDependencies": {
    "prisma": "^6.7.0",
    "typescript": "^5.8.3"
  }
}
